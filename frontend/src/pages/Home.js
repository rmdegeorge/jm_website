import React from 'react';
import styled from 'styled-components';

const HomeContainer = styled.div`

`;

const Home = (props) => {
  return (
    <HomeContainer>
      Home
    </HomeContainer>
  )
}

export default Home;
