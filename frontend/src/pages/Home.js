import React from 'react';
import styled from 'styled-components';

const HomeContainer = styled.div`

`;

const Home = (props) => {
  return (
    <HomeContainer>
      
    </HomeContainer>
  )
}

export default Home;
