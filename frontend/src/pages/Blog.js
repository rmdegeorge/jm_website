import React from 'react';
import styled from 'styled-components';

const BlogContainer = styled.div`

`;

const Blog = (props) => {
  return (
    <BlogContainer>
      Blog
    </BlogContainer>
  )
}

export default Blog;
